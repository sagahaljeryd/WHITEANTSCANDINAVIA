# WHITE ANT SCANDINAVIA — Project Memory

This directory is the small durable memory layer for future work in this repository. It contains only
verified, reusable project knowledge. It must never contain credentials, customer data, raw financial
exports or full conversation transcripts.

**Last verified:** 2026-08-29 UTC.

## Canonical project

- Repository: `sagahaljeryd/WHITEANTSCANDINAVIA`.
- Default branch: `main`.
- Project: WHITE ANT SCANDINAVIA.
- Legal storefront operator verified from the public Legal Notice: Maren Moerk AB, organisation
  number `559591-5090`.
- Canonical public storefront: `whiteantscandinavia.com`.
- Canonical Shopify shop identity: `xbpcuj-5a.myshopify.com`, shop ID `101526143302`.
- Clarté Paris is a separate brand and its connection, IDs and data are prohibited as WHITE ANT
  evidence.

## Required read order

Before work:

1. read this file;
2. read `GOALS.md`, `CURRENT_STATE.md` and `DECISIONS.md`;
3. read `LEARNINGS.md` when the task touches a known risk or past failure;
4. read the repository files required by `AGENTS.md` and the relevant specialist instructions.

## Required update flow

After work:

1. update `CURRENT_STATE.md` only with verified changes, blockers and checks;
2. add a durable choice to `DECISIONS.md` only when it is confirmed and reusable;
3. add a learning to `LEARNINGS.md` only when it will prevent a repeated error;
4. keep `GOALS.md` aligned with the approved roadmap;
5. cite the source, date and exact identifier where available.

Unknown or unavailable data is `Ej anslutet`, never zero and never guessed.
